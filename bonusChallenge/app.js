/*
 * You should write Mocha code that will test the functionalities of fizzBuzz() and gatherNamesOf() functions.
 ** Start with looking at the test/app-test.js!!!
 */

/*
  * Write a program that prints the numbers. 
      1 - numbers that are multiples of three returns “Fizz” instead of the number 
      2 - numbers that are multiples of five returns “Buzz” instead of the number. 
      3 - numbers which are multiples of three and five returns “FizzBuzz”
      4 -  and numbers that are either multiple of three or five returns the number.
      Ex: 1, 2, Fizz, 4, Buzz, Fizz, 7, 8, Fizz, ...
      Write the code as you complete each it test in the test/app-test.js. 
 */
const fizzBuzz = () => {};

/*Implement the gatherNamesOf function as needed to guarantee that:
  1- the function receives an array of objects with people names as a parameter and returns the property name value.
  Ex: The array of objects is: [{name: 'John'}, {name:'Patricia'}]. It should return ['John', 'Patricia'];
  Write the code as you complete each it test in the test/app-test.js. 
*/
const gatherNamesOf = () => {};

module.exports = { fizzBuzz, gatherNamesOf };
